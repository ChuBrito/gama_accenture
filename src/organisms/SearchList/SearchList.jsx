import React from "react";

const MOCKITEM = {
  initialDestination='',
  finalDestination,
  flyType,
  value,
  airline,
  flyInfo,
  data: {
    initialDate,
    arriveDate,
  },
};

const MOCKLIST = {};
const SearchList = (searchList) => {
  return <div></div>;
};

export default SearchList;
